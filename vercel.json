{
    "buildCommand": "pip install -r requirements.txt",
    "functions": {
        "api/index.py": {
            "maxDuration": 60,
            "includeFiles": "{templates,css,js,posts}/**"
        }
    },
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/api/index"
        }
    ]
}